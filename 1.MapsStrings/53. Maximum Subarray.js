const nums = [-2, 1, -3, 4, -1, 2, 1, -5, 4];

var maxSubArray = function (nums) {
	let maxCurrent = nums[0];
	let maxGlobal = nums[0];

	for (let i = 1; i < nums.length; i++) {
		maxCurrent = Math.max(nums[i], nums[i] + maxCurrent);
		maxGlobal = Math.max(maxCurrent, maxGlobal);
	}

	return maxGlobal;
};

console.log(maxSubArray(nums));

// Алгоритм Кадане (Kadane's Algorithm)
// Ищет максимальную сумму непрерывного подмассива в массиве (возможны отрицательные числа).

//! Каждый maxCurrent — это накопленная сумма текущего подмассива, если она выгодна.
//! Если добавление нового элемента ухудшает сумму (делает её меньше, чем сам элемент),
//!  мы сбрасываем подмассив и начинаем новый с этого элемента.

// Основная идея:
// На каждом шаге выбираем: продолжить текущий подмассив или начать новый.

// Алгоритм по шагам:
// Инициализация:

// maxCurrent = nums[0] (текущая максимальная сумма, заканчивающаяся на этом элементе)

// maxGlobal = nums[0] (глобальная максимальная сумма)

// Проход по массиву (со второго элемента):

// text
// Для каждого элемента nums[i]:
//   maxCurrent = max(nums[i], maxCurrent + nums[i])
//   maxGlobal = max(maxGlobal, maxCurrent)
// Возврат maxGlobal.

//-----------------------------------------------------------------------------------

// Ключевой момент:
// maxCurrent отвечает на вопрос:
// "Какую максимальную сумму можно получить, если подмассив обязательно заканчивается на текущем элементе?"

nums = [4, -1, 2, 1];
